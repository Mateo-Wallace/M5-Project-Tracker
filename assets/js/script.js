const projectButtonEl = document.getElementById('project-button');
const modalEl = document.getElementById('modal');

function showModal {
    modalEl.classList.remove('hide')
}














// Listening to sidebar add Project Button in order to show the Modal
projectButtonEl.addEventListener('click', showModal);
